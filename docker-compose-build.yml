services:
  portal:
    build: apps/portal
    user: "1001:1001"
    ports:
      - "3000:3000"
    env_file:
      - apps/portal/.env
    volumes:
      - public:/app/apps/portal/public
volumes:
  public:

